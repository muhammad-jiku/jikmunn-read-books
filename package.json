{
  "name": "jikmunn-read-books",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "client/**/*.{js,ts,jsx,tsx}": [
      "cd client && yarn lint-prettier"
    ],
    "client/src/**/*.{js,ts,jsx,tsx}": [
      "cd client && yarn lint-prettier"
    ],
    "server/src/**/*.{js,ts}": [
      "cd server && yarn lint-prettier"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.5",
    "lint-staged": "^15.2.10",
    "pinst": "^3.0.0"
  }
}
